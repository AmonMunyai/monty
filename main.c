#include "monty.h"

/**
 * main - monty driver code
 * @argc: no. of arguments
 * @argv: list of arguments
 * Return: always 0 (on success)
 */

int  main(int argc, char *argv[])
{

	return (EXIT_SUCCESS);
}
